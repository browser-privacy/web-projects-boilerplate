// import { fromJS } from 'immutable';
// import { selectEmailVerificationPageDomain } from '../selectors';

describe('selectEmailVerificationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
